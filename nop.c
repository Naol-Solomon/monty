#include "monty.h"
/**
 * nop- function that do not return anything
 * @top:pointer pointer to the top pointer
 * @number: number at which error occurred
 * Return: Nothing
*/
void nop(stack_t **top, unsigned int number)
{
    (void) number;
    (void) top;
}