#include "monty.h"
/**
 * stack - prints the top
 * @top: stack head
 * @number: line_number
 * Return: nothing
*/
void stack(stack_t **top, unsigned int number)
{
	(void)top;
	(void)number;
	handle.desider = 0;
}